# פרסום NOIZZ על שרת IIS (נתונים משותפים לכל המשתמשים)

מדריך זה מסביר כיצד לפרסם את NOIZZ על שרת IIS כך שכל חברי הצוות
רואים ועורכים את **אותם נתונים** (במקום עותק נפרד בכל דפדפן).

## איך זה עובד

- `noizz.html` — הממשק (אותו קובץ כמו קודם).
- `data.ashx` — מטפל (handler) קטן ב-ASP.NET ששומר את כל הנתונים
  כקובץ JSON יחיד על השרת, ומגיש אותו לכל המשתמשים.
- `web.config` — הגדרות IIS (דף ברירת מחדל, הפעלת PUT, מגבלות גודל).

הדף טוען את הנתונים מהשרת בעת הפתיחה, שומר לשרת אחרי כל שינוי,
ומרענן אוטומטית כל כ-8 שניות כדי להציג שינויים של אחרים. אם השרת
אינו זמין (למשל פתיחת הקובץ ישירות), הדף חוזר אוטומטית למצב מקומי
(LocalStorage) כמו בגרסה המקורית. מצב הסנכרון הנוכחי מוצג בסרגל
העליון: **● מסונכרן** (שרת) או **○ מצב מקומי**.

## דרישות מקדימות

1. **Windows Server עם תפקיד IIS** מותקן.
2. **ASP.NET 4.x** מופעל ב-IIS. ל-Windows Server:
   *Server Manager → Add Roles and Features → Web Server (IIS) → 
   Application Development →* סמנו **ASP.NET 4.x** (וגם
   *.NET Extensibility 4.x*). אין צורך להתקין דבר נוסף — ‎.NET Framework‎
   כבר מובנה ב-Windows.

> אין צורך ב-Visual Studio ואין שלב קומפילציה — IIS מקמפל את
> `data.ashx` אוטומטית בזמן ריצה.

## שלבי ההתקנה

1. **צרו תיקייה** לאתר, למשל `C:\inetpub\noizz`.

2. **העתיקו לתוכה את שלושת הקבצים:**
   - `noizz.html`
   - `data.ashx`
   - `web.config`

3. **צרו תת-תיקייה בשם `App_Data`** בתוך אותה תיקייה
   (`C:\inetpub\noizz\App_Data`). כאן יישמר קובץ הנתונים. אם לא
   תיצרו אותה — המטפל ייצור אותה בעצמו בכתיבה הראשונה (בתנאי שיש הרשאה).

4. **צרו אתר או אפליקציה ב-IIS Manager:**
   - *Sites → Add Website* (או *Add Application* תחת אתר קיים).
   - **Physical path:** התיקייה מסעיף 1.
   - בחרו פורט/Host name לפי הצורך (למשל פורט 80 או 8080).

5. **תנו הרשאת כתיבה לתיקיית `App_Data`** עבור זהות ה-Application Pool:
   - לחיצה ימנית על `App_Data` → *Properties → Security → Edit → Add*.
   - הזינו `IIS AppPool\<שם ה-AppPool>` (ברירת המחדל זהה לשם האתר),
     לחצו *Check Names*, ותנו הרשאת **Modify**.
   - לחלופין אפשר לתת הרשאה ל-`IIS_IUSRS`.

6. **גלשו לאתר** (למשל `http://<שם-השרת>/`). הדף ייפתח; בסרגל העליון
   אמור להופיע **● מסונכרן**. צרו משימה במחשב אחד ובדקו שהיא מופיעה
   במחשב אחר אחרי כמה שניות.

## גיבוי הנתונים בשרת

קובץ הנתונים נמצא ב-`App_Data\noizz-data.json`. אפשר:
- לגבות אותו ישירות מהשרת (העתקת הקובץ), **או**
- להשתמש בכפתור **ייצוא גיבוי (JSON)** במסך ההגדרות, שמוריד את כל
  הנתונים המשותפים. **ייבוא** או **איפוס** במצב שרת ידרסו את הנתונים
  המשותפים של כולם — תופיע אזהרה מתאימה לפני ביצוע.

## פתרון תקלות

| תופעה | סיבה אפשרית / פתרון |
|---|---|
| מופיע **○ מצב מקומי** במקום שרת | `data.ashx` לא נגיש. ודאו ש-ASP.NET 4.x מופעל ושהקובץ באותה תיקייה. |
| שמירה נכשלת (PUT 405) | WebDAV חוסם PUT. `web.config` כבר מסיר אותו; אם נדרש, הסירו את תפקיד WebDAV מ-IIS. |
| שגיאה 500 בשמירה | אין הרשאת כתיבה ל-`App_Data`. ראו שלב 5. |
| "עודכן מרחוק" קופץ הרבה | שני אנשים עורכים בו-זמנית; זה תקין — הלוח מתרענן לגרסה העדכנית. |

## הערה על עריכה בו-זמנית

המערכת שומרת את כל המסמך בכל שינוי, עם בקרת גרסאות (ETag): אם משתמש
אחר שמר בינתיים, השמירה נדחית והלוח נטען מחדש לגרסה העדכנית. בצוות
קטן (5–15 איש) זה עובד היטב; הריענון התכוף ממזער התנגשויות. אין זו
מערכת עריכה-משותפת ברמת-תו כמו Google Docs, אלא סנכרון פשוט ואמין
ברמת המסמך.
